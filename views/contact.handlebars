<div class="justify-center">
    <h3>Contact Information</h3>
    <div>{{apartment.complex.street_address}} {{apartment.complex.city}}, {{apartment.complex.state}} {{apartment.complex.zip}}</div>
</div>
<br>
{{#if logged_in}}
    <div>
        <div>Property Manager</div>
        <h4>{{apartment.complex.employee.firstname}} {{apartment.complex.employee.lastname}}</h4>
        <div>{{apartment.complex.employee.management.name}}</div>
        <div>{{apartment.complex.employee.contact_number}}</div>
    </div>
{{/if}}

<div></div>